# Kubernetes Deployment Placeholders

This directory is reserved for future Kubernetes manifests for StateSight services.

TODO:
- Add base manifests for API, worker, web, Postgres, and Redis dependencies.
- Add overlays for dev/staging/prod once baseline behavior stabilizes.
